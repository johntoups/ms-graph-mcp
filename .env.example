# Microsoft Graph MCP Server - Environment Configuration
# Copy this file to .env and fill in your values
#
# DO NOT commit .env to git - it contains secrets!

# =============================================================================
# REQUIRED: Azure AD Application Credentials
# =============================================================================
# Get these from Azure Portal > App Registrations > Your App

# Application (client) ID from Azure AD
CLIENT_ID=your-client-id-here

# Directory (tenant) ID from Azure AD
TENANT_ID=your-tenant-id-here

# =============================================================================
# OPTIONAL: Additional Configuration
# =============================================================================

# Microsoft Graph API endpoint (default: v1.0)
# GRAPH_API_ENDPOINT=https://graph.microsoft.com/v1.0

# OAuth scopes - usually set automatically by the server
# Only override if you need specific scope restrictions
# SCOPES=https://graph.microsoft.com/.default

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# =============================================================================
# NOTES
# =============================================================================
# - CLIENT_SECRET is NOT required - this server uses device code flow
# - OAuth tokens are cached in /data/.athena/credentials/graph_mcp_cache.json
# - First run will display a device code for authentication
# - ATHENA_DATA_DIR and HOME are set in docker-compose.yml, not here
